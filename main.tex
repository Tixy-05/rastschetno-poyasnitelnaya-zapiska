\input{header.tex}
\usepackage{placeins}
\raggedbottom

\newcommand*\template[1]{\text{<}#1\text{>}}
\newcommand{\enq}[1]{<<#1>>}
\newcommand{\eng}[1]{\begin{english}#1\end{english}}
\newcounter{cTheorem}
\newcounter{cDefinition}
\newcounter{cConsequent}
\newcounter{cExample}
\newcounter{cLemma}
\newcounter{cConjecture}
\newcounter{cProposition}
\newcounter{cRemark}
\theoremstyle{definition}
\newtheorem{Theorem}{Теорема}[cTheorem]
\newtheorem{Definition}{Определение}[cDefinition]
\newtheorem{Consequent}{Следствие}[cConsequent]
\newtheorem{Example}{Пример}[cExample]
\newtheorem{Lemma}{Лемма}[cLemma]
\newtheorem{Conjecture}{Гипотеза}[cConjecture]
\newtheorem{Proposition}{Утверждение}[cProposition]
\newtheorem{Remark}{Замечание}[cRemark]
\newenvironment{Proof}{\begin{proof}}{\end{proof}}
\renewcommand{\theTheorem}{\arabic{Theorem}}
\renewcommand{\theDefinition}{\arabic{Definition}}
\renewcommand{\theConsequent}{\arabic{Consequent}}
\renewcommand{\theExample}{\arabic{Example}}
\renewcommand{\theLemma}{\arabic{Lemma}}
\renewcommand{\theConjecture}{\arabic{Conjecture}}
\renewcommand{\theProposition}{\arabic{Proposition}}
\renewcommand{\theRemark}{\arabic{Remark}}
\NewDocumentCommand{\Newline}{}{\text{\\}}
\newcommand{\sequence}[2]{\ensuremath \left(#1,\ \dots,\ #2\right)}
\definecolor{mygreen}{rgb}{0,0.6,0}
\definecolor{mygray}{rgb}{0.5,0.5,0.5}
\definecolor{mymauve}{rgb}{0.58,0,0.82}
\renewcommand{\listalgorithmname}{Список алгоритмов}
\floatname{algorithm}{Листинг}
\renewcommand{\thealgorithm}{\arabic{algorithm}}
\newcommand{\refAlgo}[1]{(листинг~\ref{#1})}
\newcommand{\refImage}[1]{(рисунок~\ref{#1})}
\newcommand{\inlisting}[1]{в~листинге~\ref{#1}}
\newcommand{\Inlisting}[1]{В~листинге~\ref{#1}}
\newcommand{\algorithmicforeach}{\textbf{foreach}}
\algdef{SE}[FOR]{Foreach}{EndForeach}[1]{\algorithmicforeach\ #1\ \algorithmicdo}{\algorithmicend\ \algorithmicforeach}%
\newcommand{\onfigure}[1]{на~рисунке~\ref{#1}}
\newcommand{\onfigures}[3]{на~рисунках~\ref{#1}, \ref{#2} и~\ref{#3}}
\newcommand{\Onfigures}[3]{На~рисунках~\ref{#1}, \ref{#2} и~\ref{#3}}
\renewcommand{\theenumi}{\arabic{enumi}.}
\renewcommand{\labelenumi}{\arabic{enumi}.}
\renewcommand{\theenumii}{\arabic{enumii}}
\renewcommand{\labelenumii}{(\arabic{enumii})}
\renewcommand{\theenumiii}{\roman{enumiii}}
\renewcommand{\labelenumiii}{(\roman{enumiii})}
\renewcommand{\labelitemi}{---}
\renewcommand{\labelitemii}{---}
\newcommand{\prog}[1]{{\ttfamily\small#1}}
\newcommand{\anonsection}[1]{\cleardoublepage
\phantomsection
\addcontentsline{toc}{section}{\protect\numberline{}#1}
\section*{#1}\vspace*{2.5ex}
}
\newcommand{\sectionbreak}{\clearpage}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\renewcommand{\cftsecfont}{\normalfont\large}
\renewcommand{\cftsubsecfont}{\normalfont\large}
\setlength{\cftsecindent}{0pt}
\setlength{\cftsubsecindent}{0pt}
\setlength{\cftsubsubsecindent}{0pt}
\DeclarePairedDelimiter\abs{\lvert}{\rvert}
\DeclarePairedDelimiter\norm{\lVert}{\rVert}
\makeatletter
\DeclareTextCommandDefault{\textvisiblespace}{%
  \mbox{\kern.06em\vrule \@height.3ex}%
  \vbox{\hrule \@width.3em}%
  \hbox{\vrule \@height.3ex}}
\makeatother
\newsavebox{\spacebox}
\begin{lrbox}{\spacebox}
\verb*! !
\end{lrbox}
\newcommand{\aspace}{\usebox{\spacebox}}
\newcommand{\exprLang}{\mathcal{W}_{\text{expr}}}
\newcommand{\patLang}{\mathcal{W}_{\text{pat}}}
\makeatletter
\renewcommand*{\p@subsubsection}{}
\makeatother
\newcommand{\RL}{Рефал5-$\lambda$}
\newcommand{\Lang}{\mathcal{L}}
\newcommand{\Fact}{\mathcal{L}_{\text{fact}}}

\DeclareMathOperator{\sent}{\textbf{sent}}
\DeclareMathOperator{\pat}{\textbf{pat}}
\DeclareMathOperator{\subpat}{\textbf{subpat}}
\DeclareMathOperator{\abstr}{\textbf{abstr}}
\newcommand{\tuple}[1]{\langle #1 \rangle}
\newcommand{\s}{^\wedge\!}
\newcommand{\e}{\textnormal{\$}}

\def\EAD{\mathcal{E}_{AD}}
\def\AD{AD}

\newcommand{\leqEAD}{\preceq_{\EAD}}
\newcommand{\leEAD}{\prec_{\EAD}}

\newcommand{\litparen}{\textnormal{\texttt{(}}}
\newcommand{\litrep}{\textnormal{\texttt{)}}}
\newcommand{\edgelabel}[1]{%
  \begingroup\footnotesize
  \def\eltxt{#1}%
  \foreach \dx/\dy in {0.55/0,-0.55/0,0/0.55,0/-0.55,0.4/0.4,-0.4/0.4,0.4/-0.4,-0.4/-0.4}{%
    \rlap{\textcolor{white}{\raisebox{\dy pt}{\kern\dx pt\eltxt}}}%
  }%
  \eltxt
  \endgroup
}

\begin{document}
\sloppy

\renewcommand\contentsname{\hfill{\normalfont{СОДЕРЖАНИЕ}}\hfill}
\tableofcontents
\newpage

% 4 стр. титул, кал.план, задание, содержание

\section*{АННОТАЦИЯ} % 1 стр.

\anonsection{ВВЕДЕНИЕ} % 1 стр.

\section{Исследовательская часть}

\subsection{Обзор предметной области} % 2 стр.

Язык программирования Рефал имеет довольно большую историю, начавшуюся 
\enq{теоретически} 
еще в 1966 году, когда В.Ф. Турчин предложил
Рефал как метаязык для описания других языков программирования, а 
\enq{практически} 
в 1968 г., когда появилась первая реализация Рефала. Рефал имеет множество диалектов, но все они, по сути, являются надстройками над так называемым базисным Рефалом. Базисный рефал в некотором смысле является самым простым диалектом языка. Программы на базисном Рефале состоят из функций, каждая функция задается парами из шаблона в левой части и выражения в правой.

Единственный тип данных в Рефале --- объектное выражение. Это выражение, 
состоящее из атомарных символов (символ, число) и скобок, при этом скобки правильно вложены. 
Шаблон в левой части функции --- это выражение, в котором, помимо скобок и атомов, могут встречаться переменные. Переменные в шаблоне могут быть трех типов: $e$-переменные, $s$-переменные и $t$-переменные. $e$-переменные могут сопоставляться с любым объектным выражением, $s$-переменные сопоставляются с атомом, $t$-переменные сопоставляются либо с атомом, либо с выражением в скобках. Каждая переменная обладает именем, при этом, если в одном шаблоне есть несколько переменных с одним именем, то при подстановке они заменяются на разные значения.

Основной механизм работы Рефала --- сопоставление с образцом и переписывание выражений. При сопоставлении с образцом шаблон в левой части функции сопоставляется с объектным выражением в правой части. Если сопоставление прошло успешно, переменным присваиваются конкретные значения, затем вычисляется выражение в правой части. 
Сопоставление происходит сверху вниз, если какое-то выражение не сопоставилось ни с одним предложением, это приводит к ошибке времени выполнения.

\Inlisting{lst:IsPal} функция \texttt{IsPal} проверяет, является ли переданное ей выражение, состоящее только из атомов, палиндромом.

\begin{listing}[H]
  \caption{Пример функции на базисном Рефале}
  \label{lst:IsPal}
  \begin{minted}{refal}
  IsPal {
    s.x e.x s.x = <IsPal e.x>;
    = True;
    s.oneChar = True;
    e.otherwise = False;
  }
  \end{minted}
\end{listing}

Хотя Рефал преимущественно изучается в контексте суперкомпиляции, программы на нём можно исследовать и в контексте статического анализа. 
С одной стороны, базисный Рефал 
является чистым функциональным языком, выполнять статический анализ для которых, обычно, легче (по сравнению с императивными языками). С другой стороны, основная конструкция Рефал-программ --- приписывание --- является 
ассоциативным, что существенно осложняет задачу анализа.

\subsection{Общие определения} % 2 стр.

\begin{Definition}[Алфавит]
  Алфавитом $\Sigma$ будем называть конечное, если не указано иное, множество символов.
\end{Definition}

Будем обозначать алфавиты заглавными греческими буквами.

\begin{Definition}[Связанные со строками понятия]
  Строкой $w = w_1 w_2 \ldots w_n$ будем называть 
  конечную последовательность символов из $\Sigma$.

  Длиной строки $w$ будем называть число $n$, обозначаемое как $|w|$.

  Пустым словом будем называть строку длины 0, обозначаемую как $\varepsilon$.

  $i$-тый символ строки $w$ будем обозначать как $w[i]$.
\end{Definition}

Строки будем обозначать строчными латинскими буквами.

\begin{Definition}[Обращение строки]
Для строки $w = w_1 w_2 \ldots w_n$ будем называть ее обращением строку $w^R = w_n \ldots w_2 w_1$.
Для множества строк $W = \{w_1, w_2, \dots\}$ будем называть его обращением множество $M^R = \{w_1^R, w_2^R, \dots\}$.
\end{Definition}

\begin{Definition}[Итерация Клини]
  Множеством строк $\Sigma^*$ будем называть множество всех конечных строк над алфавитом $\Sigma$.
  $\varepsilon \in \Sigma^*$. Множество $\Sigma^* \setminus \{\varepsilon\}$ обозначим $\Sigma^+$.
\end{Definition}

\begin{Definition}[Сокращенная запись конкатенации]
  Конкатенацией строк $u = u_1 u_2 \ldots u_n$ и $v = v_1 v_2 \ldots v_m$ будем называть 
  строку $u \cdot v = u_1 u_2 \ldots u_n v_1 v_2 \ldots v_m$.

  Если $M \subset \Sigma^*$ --- некоторое множество строк и $w \in \Sigma^*$, 
  то будем обозначать как $Mw$ множество $\{mw \mid  m \in M\}$.
  Аналогично для двух множеств строк $M_1, M_2$ будем обозначать $M_1 M_2$ множество 
  $\{m_1 m_2 \mid m_i \in M_i\}$.
\end{Definition}

\begin{Definition}
  Множество $\Sigma^* \setminus W$ будем обозначать $\overline{W}$ и называть дополнением языка $W$.
\end{Definition}

Множества строк будем обозначать заглавными латинскими буквами.

\begin{Definition}[Строковый морфизм]
  Строковый морфизм $\phi\!: \Sigma^* \to \Gamma^*$ --- это функция, которая каждому слову $w \in \Sigma^*$ 
  ставит в соответствие слово $\phi(w) \in \Gamma^*$, сохраняющаяконкатенацию, т.е. $\forall w_1, w_2 \in \Sigma^* \ \phi(w_1 w_2) = \phi(w_1) \phi(w_2)$. Строковый морфизм однозначно задается набором значений на символах алфавита $\Sigma$. Если для некоторого $\sigma \in \Sigma$ не задано явным образом отображение, будем считать, что $\sigma$ отображается в себя. В таком случае будем опускать этот факт в записи областей определения и значений морфизма.
\end{Definition}

Все морфизмы в данной работе --- строковые, поэтому будем опускать слово "строковый".
Будем обозначать морфизмы строчными греческими буквами.

\begin{Example}
  Пусть $\Sigma = \Gamma = \{a, b, c\}$. Морфизм, меняющий местами $a$ и $b$, можно задать следующим образом:
  $\phi\!: \{a, b\} \to \{a, b\}$, при этом 
  \begin{align*}
    & \phi(a) \mapsto b; \\
    & \phi(b) \mapsto a.
  \end{align*}
  В таком способе записи неявно задано $\phi(c) = c$.
\end{Example}

\begin{Definition}[Множество обязательных префиксов]
Для языка $W \subseteq \Sigma^*$ его множеством обязательных префиксов будем называть множество $P \subseteq \Sigma^*$ такое, что 
$$
\forall w \in W \ \exists p \in P \ \exists v \in \Sigma^* \ (w = pv).
$$
\end{Definition}

\begin{Definition}[Обязательный инфикс]
  Для языка $W \subseteq \Sigma^*$ его обязательным инфиксом будем называть слово $f \in \Sigma^*$ такое, что 
  $$
  \forall w \in W \ \exists v, u \in \Sigma^* \ (w = vfu).
  $$
\end{Definition}
% \begin{Definition}[синтаксическая конгруэнция]
% Синтаксической конгруэнцией языка $L$ будем называть отношение эквивалентности $\sim_L$ такое, что
% $$
% u \sim_L v \iff \forall x, y \in \Sigma^* \ (xuy \in L \iff xvy \in L)
% $$
% \end{Definition}

% Класс эквивалентности некоторого $x$ в фактор-структуре будем обозначать как $[x]$

% \begin{Definition}[Синтаксический моноид]
% Фактор-множество $\Sigma^* /\! \sim_L$ с операцией конкатенации ($[x][y] = [xy]$) --- 
% синтаксический моноид языка $L$.
% \end{Definition}

% \begin{Definition}[синтаксическая полугруппа]
%   Фактор-множество $\Sigma^+ /\! \sim_L$ с операцией конкатенации ($[x][y] = [xy]$) --- 
%   синтаксическая полугруппа языка $L$.
% \end{Definition}

Для $n \in \mathbb{N}$ примем $\overline{1,n} = \{1, 2, \ldots, n\}$.

\subsection{Фактический язык шаблона} % 2 стр.

\begin{Definition}[Язык правильно вложенных слов]
  \label{Def::PLIS}
  Пусть $\Sigma_{\text{call}}$, $\Sigma_{\text{ret}}$ и $\Sigma_{\text{int}}$ --- непересекающиеся алфавиты. 
  Язык правильно вложенных слов $\mathcal{W}$ --- это наименьшее множество, 
  которое генерируется следующими конструкторами:
  \begin{align*}
  \varepsilon &\in \mathcal{W} \quad \text{(пустое слово)} \\
  \gamma &\in \mathcal{W} \quad \text{для каждого } \gamma \in \Sigma_{\text{int}} \quad \text{(внутренний символ)} \\
  w_1 \cdot w_2 &\in \mathcal{W} \quad \text{если } w_1, w_2 \in \mathcal{W}  \\
  \langle_c \; w \; \rangle_r &\in \mathcal{W} \quad \text{если } w \in \mathcal{W},\; \langle_c \in \Sigma_{\text{call}},\; \rangle_r \in \Sigma_{\text{ret}} 
  \end{align*}
\end{Definition}

Выражением будем называть слова языка правильно вложенных слов $\exprLang$, 
порожденного алфавитами $\Sigma_{\text{call}} = \{\litparen\}$, 
$\Sigma_{\text{ret}} = \{\litrep\}$ и $\Sigma_{\text{int}} = \Delta$, 
где $\Delta$ --- счетное множество символов.
Абстрактным шаблоном без повторных переменных будем называть слова языка правильно вложенных слов $\patLang$, 
порожденного алфавитами $\Sigma_{\text{call}} = \{\litparen\}$, 
$\Sigma_{\text{ret}} = \{\litrep\}$ и $\Sigma_{\text{int}} = \{e, s, t\}$.

\begin{Remark}
Формальное описание абстрактного шаблона без повторных переменных соответствует таким шаблонам языка Рефал, в которых все переменные имеют различные имена и атомарные символы не встречаются вовсе.
Например, для диалекта \RL, поддерживающего безымянные переменные,
из абстрактного шаблона можно получить обычный, заменив все символы $e$ ($s, t$)
абстрактного шаблона на символы \texttt{e.\_} (\texttt{s.\_},\texttt{t.\_}) шаблона \RL.
\end{Remark}

Введем также понятие шаблона с пронумерованными переменными: каждому шаблону $p \in \patLang$ 
поставим в соответствие слово над алфавитом $\Delta_\mathbb{N} = \{\litparen, \litrep\} \cup \{e, s, t\} \times \mathbb{N}$ такое,
что все вхождения символов $e, s, t$ в $p$ в шаблоне с пронумерованными переменными 
заменяются на буквы из $\{e, s, t\} \times \mathbb{N}$, причем $i$-тому вхождению 
символа $e$ в $p$ соответствует буква $(e, i)$ (обозначим $e_i$). Аналогично для $s$ и $t$. 
Скобки $\litparen, \litrep$ остаются на месте. Для шаблона $p$ будем обозначать соответствующий ему 
шаблон с пронумерованными переменными $p_\mathbb{N}.$

Этот формализм нужен лишь затем, чтобы формализовать понятие сопоставления с образцом.

Соответствие переменных множествам их допустимых значений было приведено 
в обзоре базисного Рефала, теперь же формализуем это соответствие с учетом, что рассматриваются только абстрактные шаблоны без повторных переменных.
Будем говорить, что выражение $w$ \textit{допускает сопоставление} с образцом $p$, 
если существует строковый морфизм $\phi\!: \Delta_\mathbb{N}^* \to \exprLang$, действующий следующим образом:
\begin{align*}
  & \phi((s, i)) \mapsto \gamma \in \Sigma_{\text{int}} \ \forall i \in \mathbb{N}; \\
  & \phi((t, i)) \mapsto \gamma \in \Sigma_{\text{int}} \text{ или }
    \phi((t, i)) \mapsto \litparen\, v \,\litrep,\ v \in \exprLang \ \forall i \in \mathbb{N}, \\
\end{align*}
при этом $\phi(p_\mathbb{N}) = w$.

\begin{Definition}[Язык шаблона]
Языком шаблона $p \in \patLang$ будем называть множество всех 
выражений $w \in \exprLang$, которые допускают сопоставление с $p$.
Обозначать это множество будем как $\Lang(p)$.
\end{Definition}

Каждая функция Рефала задается последовательностью предложений, каждое из которых
в свою очередь содержит шаблон в левой части.
Для некоторой функции $f$, содержащей $n$ предложений, будем обозначать 
$i$-тый по счету (считая сверху вниз) шаблон предложения
как $f.p_i$. Последовательность всех шаблонов функции 
$\tuple{f.p_1, f.p_2, \ldots, f.p_n}$ будем обозначать как $\pat f$.

Не всегда язык шаблона $\Lang(p)$ совпадает с множеством слов, которые могут сопоставиться с этим шаблоном, 
находящимся где-то в теле функции. Как правило, языки шаблонов в функции не являются дизъюнктными, а 
значит часть $\Lang(p)$ может буквально не доходить до шаблона $p$, обрабатываясь предложениями выше.
Для $f.p_i$ будем называть его фактическим языком множество 
$$
\Lang(f.p_i) \cap \overline{\bigcup_{j \in \overline{1, i-1}} \Lang(f.p_j)}.
$$
Будем называть фактический язык шаблона $p$ сужением шаблона и обозначать как $\Fact(p)$.

Важным частным случаем сужения является вырождение его в пустой язык. В таком случае 
имеет смысл говорить о перекрытии (экранировании) шаблона и соответствующего ему предложения.

\begin{Definition}[Перекрытие]
Предложение $i$ функции $f$ перекрывается (экранируется), если $\Fact(f.p_i) = \varnothing$.
\end{Definition}

Определим два варианта задачи распознавания перекрытия:
\begin{Definition}[Полная задача распознавания перекрытия]
Задача распознавания перекрытия в функции $f$ с $n$ предложениями 
заключается в том, чтобы для каждого шаблона $p_i \in \pat f,\ i \in \overline{1,n}$ 
найти минимальное по включению множество 
$I = \{i_1, i_2, \ldots, i_k\} \subseteq \overline{1,i-1}$ такое, что 
$p_i$ перекрывается шаблонами $f.p_{i_1}, f.p_{i_2}, \ldots, f.p_{i_k}$, 
или показать, что такого множества не существует.
\end{Definition}

\begin{Definition}[Упрощенная задача распознавания перекрытия]
  Упрощенная задача распознавания перекрытия в функции $f$
  заключается в том, чтобы для каждого шаблона $p_i \in \pat f$ определить, 
  экранируется ли он шаблонами $p_1, p_2, \ldots, p_{i-1}$.
\end{Definition}

Отметим, что решение полной задачи распознавания перекрытия неоднозначно, так 
как для одного и того же шаблона можно найти несколько минимальных по 
включению множеств индексов таких, что соответствующие им шаблоны 
перекрывают искомый. Пример такого случая приведён \inlisting{lst:ambiguous-overlapping}.
Предложение~5 перекрывается предложениями~3 и~4, а также предложением~1. 
При этом множества $\{3, 4\}$ и $\{1\}$ несравнимы по включению.

\begin{listing}[H]
\caption{Пример неоднозначного экранирования предложений Рефал-функции}
\label{lst:ambiguous-overlapping}
\begin{minted}{refal}
F {
    (t._ t._) e._ = ...;          /* 1 */
    e._ (s._ e._ s._) e._ = ...;  /* 2 */
    e._ s._ (t._) = ...;          /* 3 */
    e._ (e._) (t._) = ...;        /* 4 */
    (t._ t._) t._ (t._) = ...;    /* 5 */
}
\end{minted}
\end{listing}

\subsection{Регулярное представление шаблонов} % 0.5 стр. 

Регулярное представление шаблона представляет особый интерес, поскольку, в сравнении 
с контекстно-свободными языками (в частности, языками с магазинных автоматов, управляемых входом), 
коими являются $\exprLang$ и $\patLang$, регулярные языки более просты для анализа. 

\subsubsection{Каскадное произведение автоматов} % 3.5 стр.

Рассмотрим основную теоретическую конструкцию, 
позволяющую извлекать регулярное представление шаблонов --- каскадное произведение автоматов:

\begin{Definition}[Каскадное произведение автоматов]
  Пусть $A_1 = \langle Q_1, \Sigma, \delta_1, q_{0_1}, F_1 \rangle$ и
  $A_2 = \langle Q_2, \Sigma \times Q_1, \delta_2, q_{0_2}, F_2 \rangle$ --- детерминированные конечные автоматы.
  \emph{Каскадным произведением} $A_1 \circ A_2$ называется ДКА $\langle Q, \Sigma, \delta, q_0, F \rangle$, где
  \begin{align*}
    Q &= Q_1 \times Q_2, \\
    q_0 &= (q_{0_1}, q_{0_2}), \\
    F &= F_1 \times F_2, \\
    \delta((q_1, q_2), a) &= \bigl( \delta_1(q_1, a),\; \delta_2(q_2, (a, q_1)) \bigr).
  \end{align*}
\end{Definition}

Иногда будем просто говорить о каскаде (двух и более автоматов), подразумевая каскадное произведение автоматов.

Определение, данное выше, является вариантом прямого произведения автоматов. Как и многие другие операции над ДКА, использующие прямое произведение (например, пересечение и объединение), каскадное произведение можно описать как систему из двух автоматов, 
работающих параллельно на одном и том же входном слове. В случае каскада двух автоматов между ними проявляется односторонняя зависимость:
оба автомата синхронно читают посимвольно входное слово $w \in \Sigma^*$, при этом первый автомат 
$A$ работает автономно над алфавитом $\Sigma$, а второй автомат, помимо символа $w[i]$, 
использует текущее (до перехода по $w[i]$) состояние первого автомата $q$, т.е. работает над алфавитом $\Sigma \times Q_1$.

Основная идея заключается в делегировании распознавания вложенных подшаблонов элементам каскада.

Введем искомый алфавит $\Gamma_0 = \{b, s, \litparen, \litrep\}$. $b$ является мета-символом, 
соответствующим словам $\exprLang \ni w = \litparen\, v \,\litrep,\ v \in \exprLang$, $s$ также является мета-символом, 
соответствующим словам $\exprLang \ni w = \gamma \in \Sigma_{\text{int}}$.
Будем рассматривать регулярные выражения $r$ над алфавитом $\Gamma_0$ и ДКА $A$ над этим же алфавитом. 
Будем обозначать множество слов $\{w \mid w \in \Gamma_0^*\}$, принадлежащие языку $r$ ($A$), как $R(r)$ ($R(A)$).
Отклонение от стандартного обозначения через $\Lang$ сделано во избежание путаницы: каждое слово языка $r$ ($A$) 
в силу смысла символов $\gamma \in \Gamma_0$ само задает некоторый язык слов-выражений из $\exprLang$.
Для слова $u \in R(r)$ поставим ему в соответствие слово $u_\mathbb{N}$ над 
$\Gamma_\mathbb{N} = \{\litparen, \litrep\} \cup \{b, s\} \times \mathbb{N}$ с пронумерованными переменными, 
аналогично тому, как это было сделано для шаблонов $p \in \patLang$ и $p_\mathbb{N}$.
Будем говорить, что $w$ допускает сопоставление с $r$, если
$$
\exists u \in R(r)\ \exists \phi\!: \{b, s\} \times \mathbb{N} \to \exprLang \mid \phi(u_\mathbb{N}) = w,
$$
при этом
\begin{align*}
& \phi((b, i)) \mapsto \litparen\, w \,\litrep \in \exprLang \forall i \in \mathbb{N}; \\
& \phi((s, i)) \mapsto \gamma \in \Sigma_{\text{int}} \forall i \in \mathbb{N};
\end{align*}

Множество слов $w \in \exprLang$, допускающих сопоставление с $r$, будем обозначать как $\Lang(r)$.
Любой шаблон $p \in \patLang$ можно представить в виде регулярного выражения $r$ 
над алфавитом $\Gamma$ так, что $\Lang(p) = \Lang(r)$.
Данный факт следует непосредственно из определения самих $e, t, s$-символов, 
ниже представлено соответствие между символами и регулярными выражениями:
\begin{itemize}
\item $e$-символ соответствует выражению $(b|s)^*$;
\item $t$-символ соответствует выражению $(b|s)$;
\item $s$-символ соответствует выражению $s$.
\end{itemize}

Введем функцию глубины вложенности на элементах языка правильно вложенных слов:
$$
d(w, i) = \begin{cases}
  0,\ i = 0 \\
  d(w, i-1) + 1,\ w[i] \in \Sigma_{\text{call}} \\
  d(w, i-1) - 1,\ w[i] \in \Sigma_{\text{ret}} \\
  d(w, i-1),\ w[i] \in \Sigma_{\text{int}} \\
\end{cases}
$$
Тогда максимальным уровнем вложенности некоторого слова $w$ будет
$$\max d(w) = \max_{i \in \overline{1,\lvert w\rvert}} d(w[i]).$$
Подшаблоном $p$ уровня вложенности $k$ (или просто $k$-подшаблоном) будем называть подслово $w[i:j]$ такое, что
$$
d(w, i) = k \land d(w, j) = k - 1 \land \forall m \in \overline{i+1,j-1} \ d(w, m) \geq k - 1.
$$
Обозначим множество всех $k$-подшаблонов $p$ как $\subpat_k(p)$.

Рассмотрим теперь теоретическую конструкцию каскада по данному шаблону $p$.
Сначала для $k = max(p)$ построим регулярные выражения $r_i$ над алфавитом $\Gamma_0$ по правилам, 
описанным выше, для всех $k$-подшаблонов $p_i$; далее построим соответствующие ДКА $A_i$.

Рассмотрим ДКА-объединение $A$ всех $A_i$ с размеченными состояниями: каждое финальное состояние $f$ автомата $A$ помечается 
множеством $\alpha \subset \subpat_k(p)$, если все пути в автомате $A$ до состояния $f$ принадлежат языкам регулярных выражений, соответствующим $k$-подшаблонам $p_j \in \alpha$. Чтобы автомат корректно работал только с $k$-шаблонами, строится композиция автомата $A$ с автоматом-лифтом на уровень скобочной вложенности $k$. Начальным состоянием $A$ становится начальное состояние лифта, последний \enq{этаж} лифта совмещается с бывшим начальным состоянием $A$. Пример автомата-лифта для уровня~2 приведён \onfigure{fig:lift-level2}.

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{width=0.75\textwidth,center}
  \begin{tikzpicture}[
    >=Stealth,
    auto,
    font=\footnotesize,
    initial text={},
    initial by arrow,
    initial where=left,
    initial distance=10mm,
    every state/.style={thick, fill=gray!10, circular drop shadow,
      minimum size=10mm, inner sep=0pt, font=\scriptsize},
    lift state/.style={fill=red!15},
    every loop/.style={min distance=8mm, looseness=5},
  ]
    \def\hdist{4.5cm}

    \node[state, lift state, solid, initial] (q0) at (0, 0) {};
    \node[state, lift state, solid] (q1) at (\hdist, 0) {};
    \node[state, lift state] (q2) at (2*\hdist, 0) {};

    \path[->]
      (q0) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (q1)
      (q0) edge[loop above] node[pos=0.12, above=5pt] {\edgelabel{$s$}} ()
      (q1) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (q2)
      (q1) edge[loop above] node[pos=0.12, above=5pt] {\edgelabel{$s$}} ()
      (q1) edge[bend left=35] node[midway, above=-1pt] {\edgelabel{$\litrep$}} (q0)
      (q2) edge[bend left=35] node[midway, above=-1pt] {\edgelabel{$\litrep$}} (q1);
  \end{tikzpicture}
  \end{adjustbox}
  \caption{Автомат-лифт для уровня вложенности~$2$.}
  \label{fig:lift-level2}
\end{figure}

Полученный автомат $A$ является управляющим автоматом каскада. Если $k > 1$, то следующий элемент 
каскада будет управляться символом входного слова и состоянием $A$. Для построения управляемого автомата $B$ необходимо рассмотреть 
множество $\subpat_{k-1}$, его элементы включают себя $k$-шаблоны в качестве подшаблонов. Для принятия решения о переходе по этим $k$-шаблонам автомат $B$ использует состояния автомата $A$, аннотации финальных состояний $A$ позволяют автомату $B$ получать необходимую информацию о том, какие шаблоны прочитаны $A$.

Итак, конструкция выше задает пару автоматов, каскадное произведение которых $A \circ B$ распознает $k-1$-шаблоны. 
Повторяя процедуру выше для $k, k-1, \dots, 1$ -шаблонов, получаем каскад $A_{k} \circ A_{k-1} \circ \dots \circ A_{0}$ с аннтоациями $1-шаблонов$.

При решении задачи экранирования и задачи порождения документации необходимо различать множество $\bigcup_i \subpat_k(p_i)$ всех $k$-подшаблонов всех шаблонов 
некоторой функции $f.$
Конструкция выше тривиально обобщается на несколько шаблонов: для всех $k$ вместо $\subpat_k(p)$ конкретного шаблона рассматривается объединенное множество $\bigcup_i \subpat_k(p_i)$. Теперь из ДКА $0$-шаблонов можно получить ДКА каждого 
шаблона по отдельности, оставив финальными только те состояния, в аннотациях которых присутствует данный шаблон. 

Рассмотрим пример функции, приведённой \inlisting{lst:f}.
\begin{listing}[H]
\caption{Пример последовательности образцов с вложенными скобочными уровнями}
\label{lst:f}
\begin{minted}{refal}
f {
  t.first (e.x (s.y) s.z) = ...;
  ((t.x) e.y s.z) s.last = ...;
}
\end{minted}
\end{listing}
Для примера \inlisting{lst:f} шаблонами уровня вложенности~2 будут \texttt{s}, \texttt{t}; уровня~1 --- \texttt{e}\,\litparen\,\texttt{s}\,\litrep\,\texttt{s}, \litparen\,\texttt{t}\,\litrep\,\texttt{e}\,\texttt{s}; без скобочной вложенности --- \texttt{t}\,\litparen\,\texttt{e}\,\litparen\,\texttt{s}\,\litrep\,\texttt{s}\,\litrep, \litparen\,\litparen\,\texttt{t}\,\litrep\,\texttt{e}\,\texttt{s}\,\litrep\,\texttt{s}.
Обозначения шаблонов см. в таблице~\ref{tab:lst-f-patterns}.

\begin{table}[htbp]
\centering
\caption{Шаблоны примера (листинг~\ref{lst:f}) и обозначения в аннотациях состояний ДКА}
\label{tab:lst-f-patterns}
\begin{tabular}{@{}lcc@{}}
\toprule
Шаблон & Уровень вложенности & Обозначение \\
\midrule
\texttt{s} & 2 & $1$ \\
\texttt{t} & 2 & $2$ \\
\texttt{e}\,\litparen\,\texttt{s}\,\litrep\,\texttt{s} & 1 & $\mathrm{I}$ \\
\litparen\,\texttt{t}\,\litrep\,\texttt{e}\,\texttt{s} & 1 & $\mathrm{II}$ \\
\texttt{t}\,\litparen\,\texttt{e}\,\litparen\,\texttt{s}\,\litrep\,\texttt{s}\,\litrep & 0 & $A$ \\
\litparen\,\litparen\,\texttt{t}\,\litrep\,\texttt{e}\,\texttt{s}\,\litrep\,\texttt{s} & 0 & $B$ \\
\bottomrule
\end{tabular}
\end{table}

\Onfigures{fig:annotated-recognizer-level2}{fig:cascade-lift-example}{fig:cascade-simplification-example} 
представлены элементы каскада автоматов. Состояния 
лифта выделены красным, состояния аннотированного ДКА-объединения имеют пунктирую границу. 
Злоупотребляя нотацией, будем полагать, что во всех состояниях младших аннотированных ДКА 
для $k$-шаблонов, у которых нет явного перехода по открывающей скобке, находится лифт на 
$d-k$ уровней, где $d$ --- глубина наиболее вложенного подшаблона в функции. Такая 
конструкция необходима, чтобы младший автомат выходил из ожидания по корректной 
закрывающей скобке. Кроме того, из всех видимых состояний, из которых нет перехода по 
закрывающей скобке, неявно присутствует переход в состояние-предпоследний \enq{этаж} лифта.
Аннотированные состояния помечены финальными с целью подчеркнуть значимость сигналов, порождаемых этими состояниями, для следующего автомата в каскаде --- для младшего автомата наибольший интерес преставляют именно аннотированные состояния.
Большинство автоматов имеют полную функцию переходов, но там, где она неполна, предполагаем существование ловушки --- все отсутствующие переходы ведут в нее, выйти из ловушки можно только по закрывающей скобке в состояние лифта.

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.55\textwidth,center}
  \begin{tikzpicture}[
    >=Stealth,
    auto,
    font=\footnotesize,
    initial text={},
    initial by arrow,
    initial where=above,
    initial distance=7mm,
    every state/.style={thick, fill=gray!10, circular drop shadow,
      minimum size=7.5mm, inner sep=0pt, font=\scriptsize},
    lift state/.style={fill=red!15},
    every loop/.style={min distance=6mm, looseness=5},
  ]
    \begin{scope}[
      every state/.append style={dashed},
    ]
      \def\colsep{2.2cm}
      \def\vdist{1.35cm}

      \node[state, lift state, solid, initial] (q0) at (0, 2*\vdist) {};
      \node[state, lift state, solid] (q1) at (0, \vdist) {};
      \node[state, lift state] (q23) at (\colsep, 0) {};
      \node[state, accepting] (p1) at (2*\colsep, 0.45*\vdist) {$1$};
      \node[state, accepting] (p2) at (2*\colsep, -0.45*\vdist) {$2$};
      \node[font=\scriptsize, above=3pt of p1] {$\{1, 2\}$};
      \node[font=\scriptsize, below=3pt of p2] {$\{2\}$};

      \path[->]
        (q0) edge node[midway, left=-1pt] {\edgelabel{$\litparen$}} (q1)
        (q0) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
        (q1) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (q23)
        (q1) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
        (q1) edge[bend left=35] node[midway, left=-1pt] {\edgelabel{$\litrep$}} (q0)
        (q23) edge[bend left=35] node[midway, left=-1pt] {\edgelabel{$\litrep$}} (q1)
        (q23) edge node[pos=0.55, above=-1pt] {\edgelabel{$s$}} (p1)
        (q23) edge node[pos=0.42, right=-1pt] {\edgelabel{$b$}} (p2);
    \end{scope}
  \end{tikzpicture}
  \end{adjustbox}
  \caption{Аннотированный распознаватель для шаблонов уровня вложенности~$2$.}
  \label{fig:annotated-recognizer-level2}
\end{figure}
\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.55\textwidth,center}
  \begin{tikzpicture}[
    >=Stealth,
    auto,
    font=\footnotesize,
    initial text={},
    initial by arrow,
    initial where=above,
    initial distance=7mm,
    every state/.style={thick, fill=gray!10, circular drop shadow,
      minimum size=7.5mm, inner sep=0pt, font=\scriptsize},
    lift state/.style={fill=red!15},
    no close state/.style={fill=gray!10},
    every loop/.style={min distance=6mm, looseness=5},
  ]
    \begin{scope}[
      every state/.append style={dashed},
      initial state/.style={fill=red!30, draw=red, thick, solid},
    ]
    \def\colsep{2.2cm}
    \def\rowsep{2.625cm}

    \coordinate (c0) at (0,0);
    \coordinate (c1) at (\colsep,0);
    \coordinate (c2) at (2*\colsep,0);
    \coordinate (r0) at (0,0);
    \coordinate (r1) at (0,\rowsep);
    \coordinate (r2) at (0,2*\rowsep);
    \coordinate (r3) at (0,3*\rowsep);
    \coordinate (r4) at (0,4*\rowsep);

    \node[state]              (n04) at (c0 |- r4) {};
    \node[state, no close state] (n14) at (c1 |- r4) {};
    \node[state, no close state, accepting] (n24) at (c2 |- r4) {$1$};
    \node[state, initial, lift state, solid] (n03) at (c0 |- r3) {};
    \node[state, no close state] (n13) at (c1 |- r3) {};
    \node[state, lift state]  (n02) at (c0 |- r2) {};
    \node[state]              (n12) at (c1 |- r2) {};
    \node[state, no close state, accepting] (n22) at (c2 |- r2) {$3$};
    \node[state, no close state] (n01) at (c0 |- r1) {};
    \node[state]              (n11) at (c1 |- r1) {};
    \node[state, no close state] (n21) at (c2 |- r1) {};
    \node[state, no close state, accepting] (n00) at (c0 |- r0) {$2$};
    \node[font=\scriptsize, above=3pt of n24] {$\{\mathrm{I}\}$};
    \node[font=\scriptsize, right=3pt of n22] {$\{\mathrm{I},\,\mathrm{II}\}$};
    \node[font=\scriptsize, below=3pt of n00] {$\{\mathrm{II}\}$};

    \path[->]
      (n04) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litrep;\,1$}} (n14)
      (n14) edge node[pos=0.55, above=-1pt] {\edgelabel{$s$}} (n24)
      (n24) edge[bend left=30] node[pos=0.45, left=-1pt] {\edgelabel{$\litparen$}} (n13)
      (n14) edge node[midway, right=-1pt] {\edgelabel{$\litparen$}} (n13)
      (n24) edge[loop right] node[pos=0.12, right=5pt] {\edgelabel{$s$}} ()
      (n04) edge[bend left=22] node[pos=0.38, right=-1pt] {\edgelabel{$\litrep;\,\overline{1}$}} (n13)
      (n13) edge[bend left=22] node[pos=0.38, left=-1pt] {\edgelabel{$\litparen$}} (n04)
      (n13) edge[loop right] node[pos=0.12, right=5pt] {\edgelabel{$s$}} ()
      (n03) edge node[midway, left=-1pt] {\edgelabel{$\litparen$}} (n02)
      (n02) edge[bend left=35] node[midway, left=-1pt] {\edgelabel{$\litrep$}} (n03)
      (n03) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
      (n02) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
      (n02) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (n12)
      (n12) edge node[midway, left=-1pt] {\edgelabel{$\litrep;\,\overline{1,2}$}} (n13)
      (n22) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (n12)
      (n21) edge node[midway, left=-1pt] {\edgelabel{$s$}} (n22)
      (n12) edge[bend left=22] node[pos=0.42, right=-1pt] {\edgelabel{$\litrep;\,2$}} (n01)
      (n12) edge node[pos=0.42, right=-1pt] {\edgelabel{$\litrep;\,1$}} (n21)
      (n11) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litrep;\,1$}} (n21)
      (n21) edge[bend left=15] node[pos=0.45, below=1pt] {\edgelabel{$\litparen$}} (n11)
      (n01) edge[bend left=22] node[pos=0.42, below=2pt] {\edgelabel{$\litparen$}} (n12)
      (n01) edge node[midway, left=-1pt] {\edgelabel{$s$}} (n00)
      (n00) edge[bend right=20] node[pos=0.5, below=-1pt, right=1pt] {\edgelabel{$\litparen$}} (n11)
      (n00) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ();
    \draw[->] (n22.south east)
      .. controls ($(n22.south east)+(0.55cm,-0.25cm)$)
                  and ($(n21.east)+(2.2cm,-0.9cm)$) ..
      node[pos=0.46, right=1pt] {\edgelabel{$s$}} (n00.east);
    \draw[->] (n11.west) -- node[below=1pt, pos=0.34, xshift=-2pt, anchor=north] {\edgelabel{$\litrep;\,\overline{1}$}} (n01.east);
    \end{scope}
  \end{tikzpicture}
  \end{adjustbox}
  \caption{Пример аннотированного ДКА для 1-шаблонов.}
  \label{fig:cascade-lift-example}
\end{figure}
\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=0.55\textwidth,center}
  \begin{tikzpicture}[
    >=Stealth,
    auto,
    font=\footnotesize,
    initial text={},
    initial by arrow,
    initial where=above,
    initial distance=7mm,
    every state/.style={thick, fill=gray!10, circular drop shadow,
      minimum size=7.5mm, inner sep=0pt, font=\scriptsize},
    every loop/.style={min distance=6mm, looseness=5},
  ]
    \begin{scope}[
      every state/.append style={dashed},
    ]
      \def\colsep{2.2cm}
      \def\vdist{1.35cm}

      \node[state, initial] (q0) at (0, 2*\vdist) {};
      \node[state] (q1) at (0, \vdist) {};
      \node[state] (q2) at (\colsep, \vdist) {};
      \node[state] (q3) at (0, 0) {};
      \node[state] (q4) at (2*\colsep, \vdist) {};
      \node[state, accepting] (qA) at (2*\colsep, 0) {};
      \node[state, accepting] (qB) at (0, -\vdist) {};
      \node[font=\scriptsize, below=3pt of qA] {$\{A\}$};
      \node[font=\scriptsize, below=3pt of qB] {$\{B\}$};

      \path[->]
        (q0) edge node[midway, left=-1pt] {\edgelabel{$\litparen$}} (q1)
        (q1) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litrep;\,\overline{2,3}$}} (q2)
        (q1) edge node[midway, left=-1pt] {\edgelabel{$\litrep;\,2,3$}} (q3)
        (q2) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (q4)
        (q3) edge node[midway, left=-1pt] {\edgelabel{$s$}} (qB)
        (q4) edge node[midway, right=-1pt] {\edgelabel{$\litrep;\,1$}} (qA);
      \draw[->] (q3.east) to[out=0, in=225, looseness=0.85]
        node[pos=0.48, below=1pt] {\edgelabel{$\litparen$}} (q4.south west);
    \end{scope}
  \end{tikzpicture}
  \end{adjustbox}
  \caption{Последний элемент каскада уровня~0.}
  \label{fig:cascade-simplification-example}
\end{figure}

\subsubsection{Переход от каскада к ДКА} % 2.5 стр.
Конструкция выше является теоретическим обоснованием подхода, описанного ниже. Покажем, 
что от явного построения каскада можно избавиться вовсе и обойтись лишь построением аннотированного 
ДКА-объединения для подшаблонов всех уровней вложенности. 

Рассмотрим структуру любого управляемого элемента каскада. В любом состоянии после перехода по открывающей скобке 
управляемый автомат "ожидает" закрывающую скобку нужного уровня, фактически игнорируя все символы, поступающие на вход до нее. 
Этот факт потребуется в дальнейшем.
 
Введем на множестве состояний $Q$ аннотированного автомата отнощение эквивалентности $\sim_\alpha$, для $q_1, q_2 \in Q$ имеем $q_1 \sim_\alpha q_2$ тогда, и только тогда, когда аннотация $q_1$ совпадает с $q_2$. Будем называть элементы этого фактора $b_0, b_1, \dots, b_n$.

Заметим, что все состояния элемента каскада с одинаковыми аннотациями неразличимы с точки зрения элемента младшего уровня, а также то, что состояния с уникальной аннотацией дизъюнктен относительно других языков путей к другим аннотациям. Выражаясь немного неформально, язык $b_i$ не пересекается с языками $b_j,\ j \neq i$. 

Воспользовавшись наблюдениями выше, можно свести каскад к построению отдельных ДКА в различных алфавитах по следующим правилам:
\begin{enumerate}
\item Первый управляющий автомат $A_k = \tuple{Q, q_0, F, \delta}$ над $\{s, b\}$ строится как и ранее.
\item Строится фактор-множество $Q / \sim_\alpha$, при этом к нему добавляется элемент $\varnothing$, если в $A_k$
есть хоть одно непринимающее полезное состояние.
\item Младший элемент каскада строится над алфавитом $s, b_0, b_1, \dots, b_n$ следующим образом --- по шаблону 
все так же можно построить регулярное выражение, но символ $b$ теперь заменяется набором $b_0, b_1, \dots, b_n$. По регулярному выражению построить ДКА.
\item Если еще не построен ДКА для $0$-шаблонов, перейти к шагу 2, иначе завершить построение.
\end{enumerate}

Таким образом, в данной конструкции бремя различия несовпадающих вложенных подшаблонов ложится на алфавит. 
Конечным результатом алгоритма, следующего правилам выше, будет ДКА искомого шаблона в некотором алфавите $s, b_0, b_1, \dots, b_n.$

\Onfigures{fig:simplified-pair-level2}{fig:simplified-pair-level1}{fig:simplified-pair-level0} представлен пример упрощенной конструкции ДКА справа рядом с полным каскадом слева. Рассматривается функция \texttt{f} из примера \inlisting{lst:f}, обозначения шаблонов см. в таблице~\ref{tab:lst-f-patterns}. 
В каскаде синим цветом выделены состояния, \enq{оставшиеся} в упрощенной конструкции.

\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=\textwidth,center}
  \begin{tikzpicture}[
    >=Stealth,
    auto,
    font=\footnotesize,
    initial text={},
    initial by arrow,
    initial where=above,
    initial distance=7mm,
    every state/.style={thick, fill=gray!10, circular drop shadow,
      minimum size=7.5mm, inner sep=0pt, font=\scriptsize},
    lift state/.style={fill=red!15},
    no close state/.style={fill=blue!15},
    every loop/.style={min distance=6mm, looseness=5},
  ]
    \begin{scope}[
      every state/.append style={dashed},
    ]
      \def\colsep{2.2cm}
      \def\vdist{1.35cm}

      \node[state, lift state, solid, initial] (q0) at (0, 2*\vdist) {};
      \node[state, lift state, solid] (q1) at (0, \vdist) {};
      \node[state, lift state] (q23) at (\colsep, 0) {};
      \node[state, no close state, accepting] (p1) at (2*\colsep, 0.45*\vdist) {$1$};
      \node[state, no close state, accepting] (p2) at (2*\colsep, -0.45*\vdist) {$2$};
      \node[font=\scriptsize, above=3pt of p1] {$\{1, 2\}$};
      \node[font=\scriptsize, below=3pt of p2] {$\{2\}$};

      \path[->]
        (q0) edge node[midway, left=-1pt] {\edgelabel{$\litparen$}} (q1)
        (q0) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
        (q1) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (q23)
        (q1) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
        (q1) edge[bend left=35] node[midway, left=-1pt] {\edgelabel{$\litrep$}} (q0)
        (q23) edge[bend left=35] node[midway, left=-1pt] {\edgelabel{$\litrep$}} (q1)
        (q23) edge node[pos=0.55, above=-1pt] {\edgelabel{$s$}} (p1)
        (q23) edge node[pos=0.42, right=-1pt] {\edgelabel{$b$}} (p2);
    \end{scope}

    \begin{scope}[xshift=7.2cm]
      \def\colsep{2.2cm}
      \def\vdist{1.35cm}

      \node[state, initial] (r0) at (0, 0) {};
      \node[state, accepting] (r1) at (\colsep, 0) {$1$};
      \node[state, accepting] (r2) at (0.5*\colsep, -\vdist) {$2$};
      \node[font=\scriptsize, below=3pt of r1] {$\{1, 2\}$};
      \node[font=\scriptsize, below=3pt of r2] {$\{2\}$};

      \path[->]
        (r0) edge node[pos=0.55, above=-1pt] {\edgelabel{$s$}} (r1)
        (r0) edge node[pos=0.42, right=-1pt] {\edgelabel{$b$}} (r2);
    \end{scope}
  \end{tikzpicture}
  \end{adjustbox}
  \caption{Аннотированный распознаватель для шаблонов уровня вложенности~$2$ (слева). Справа --- тот же распознаватель без лифта над алфавитом $\{b, s\}$.}
  \label{fig:simplified-pair-level2}
\end{figure}
\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=\textwidth,center}
  \begin{tikzpicture}[
    >=Stealth,
    auto,
    font=\footnotesize,
    initial text={},
    initial by arrow,
    initial where=above,
    initial distance=7mm,
    every state/.style={thick, fill=gray!10, circular drop shadow,
      minimum size=7.5mm, inner sep=0pt, font=\scriptsize},
    lift state/.style={fill=red!15},
    no close state/.style={fill=blue!15},
    every loop/.style={min distance=6mm, looseness=5},
  ]
    \begin{scope}[
      xshift=-1.2cm,
      every state/.append style={dashed},
      initial state/.style={fill=red!30, draw=red, thick, solid},
    ]
    \def\colsep{2.2cm}
    \def\rowsep{2.625cm}

    \coordinate (c0) at (0,0);
    \coordinate (c1) at (\colsep,0);
    \coordinate (c2) at (2*\colsep,0);
    \coordinate (r0) at (0,0);
    \coordinate (r1) at (0,\rowsep);
    \coordinate (r2) at (0,2*\rowsep);
    \coordinate (r3) at (0,3*\rowsep);
    \coordinate (r4) at (0,4*\rowsep);

    \node[state]              (n04) at (c0 |- r4) {};
    \node[state, no close state] (n14) at (c1 |- r4) {};
    \node[state, no close state, accepting] (n24) at (c2 |- r4) {$1$};
    \node[state, initial, lift state, solid] (n03) at (c0 |- r3) {};
    \node[state, no close state] (n13) at (c1 |- r3) {};
    \node[state, lift state]  (n02) at (c0 |- r2) {};
    \node[state]              (n12) at (c1 |- r2) {};
    \node[state, no close state, accepting] (n22) at (c2 |- r2) {$3$};
    \node[state, no close state] (n01) at (c0 |- r1) {};
    \node[state]              (n11) at (c1 |- r1) {};
    \node[state, no close state] (n21) at (c2 |- r1) {};
    \node[state, no close state, accepting] (n00) at (c0 |- r0) {$2$};
    \node[font=\scriptsize, above=3pt of n24] {$\{\mathrm{I}\}$};
    \node[font=\scriptsize, right=3pt of n22] {$\{\mathrm{I},\,\mathrm{II}\}$};
    \node[font=\scriptsize, below=3pt of n00] {$\{\mathrm{II}\}$};

    \path[->]
      (n04) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litrep;\,1$}} (n14)
      (n14) edge node[pos=0.55, above=-1pt] {\edgelabel{$s$}} (n24)
      (n24) edge[bend left=30] node[pos=0.45, left=-1pt] {\edgelabel{$\litparen$}} (n13)
      (n14) edge node[midway, right=-1pt] {\edgelabel{$\litparen$}} (n13)
      (n24) edge[loop right] node[pos=0.12, right=5pt] {\edgelabel{$s$}} ()
      (n04) edge[bend left=22] node[pos=0.38, right=-1pt] {\edgelabel{$\litrep;\,\overline{1}$}} (n13)
      (n13) edge[bend left=22] node[pos=0.38, left=-1pt] {\edgelabel{$\litparen$}} (n04)
      (n13) edge[loop right] node[pos=0.12, right=5pt] {\edgelabel{$s$}} ()
      (n03) edge node[midway, left=-1pt] {\edgelabel{$\litparen$}} (n02)
      (n02) edge[bend left=35] node[midway, left=-1pt] {\edgelabel{$\litrep$}} (n03)
      (n03) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
      (n02) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ()
      (n02) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (n12)
      (n12) edge node[midway, left=-1pt] {\edgelabel{$\litrep;\,\overline{1,2}$}} (n13)
      (n22) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (n12)
      (n21) edge node[midway, left=-1pt] {\edgelabel{$s$}} (n22)
      (n12) edge[bend left=22] node[pos=0.42, right=-1pt] {\edgelabel{$\litrep;\,2$}} (n01)
      (n12) edge node[pos=0.42, right=-1pt] {\edgelabel{$\litrep;\,1$}} (n21)
      (n11) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litrep;\,1$}} (n21)
      (n21) edge[bend left=15] node[pos=0.45, below=1pt] {\edgelabel{$\litparen$}} (n11)
      (n01) edge[bend left=22] node[pos=0.42, below=2pt] {\edgelabel{$\litparen$}} (n12)
      (n01) edge node[midway, left=-1pt] {\edgelabel{$s$}} (n00)
      (n00) edge[bend right=20] node[pos=0.5, below=-1pt, right=1pt] {\edgelabel{$\litparen$}} (n11)
      (n00) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$s$}} ();
    \draw[->] (n22.south east)
      .. controls ($(n22.south east)+(0.55cm,-0.25cm)$)
                  and ($(n21.east)+(2.2cm,-0.9cm)$) ..
      node[pos=0.46, right=1pt] {\edgelabel{$s$}} (n00.east);
    \draw[->] (n11.west) -- node[below=1pt, pos=0.34, xshift=-2pt, anchor=north] {\edgelabel{$\litrep;\,\overline{1}$}} (n01.east);
    \end{scope}

    \begin{scope}[xshift=8.6cm]
      \def\colsep{2.2cm}
      \def\rowsep{2.625cm}
      \def\lshift{0.85}

      \coordinate (c0) at (0,0);
      \coordinate (c1) at (\colsep,0);
      \coordinate (c2) at (2*\colsep,0);
      \coordinate (r0) at (0,0);
      \coordinate (r1) at (0,\rowsep);
      \coordinate (r2) at (0,2*\rowsep);
      \coordinate (r3) at (0,3*\rowsep);
      \coordinate (r4) at (0,4*\rowsep);

      \node[state, initial]   (s0) at (c0 |- r4) {};
      \node[state]            (s1) at (c1 |- r4) {};
      \node[state]            (s2) at (c2 |- r4) {};
      \node[state, accepting] (s3) at (c2 |- r3) {$1$};
      \node[state]            (l)  at (-\lshift*\colsep, 3*\rowsep) {};
      \node[state]            (r)  at (\lshift*\colsep, 3*\rowsep) {};
      \node[state, accepting] (m2) at (c0 |- r2) {$3$};
      \node[state, accepting] (b)  at (c0 |- r0) {$2$};
      \node[font=\scriptsize, right=3pt of s3] {$\{\mathrm{I}\}$};
      \node[font=\scriptsize, left=3pt of m2] {$\{\mathrm{I},\,\mathrm{II}\}$};
      \node[font=\scriptsize, below=3pt of b] {$\{\mathrm{II}\}$};

      \path[->]
        (s0) edge node[pos=0.55, above=-1pt] {\edgelabel{$b_0$}} (s1)
        (s0) edge node[midway, left=-1pt] {\edgelabel{$b_1$}} (l.north)
        (s0) edge node[midway, right=-1pt] {\edgelabel{$b_2$}} (r)
        (s1) edge[loop above] node[pos=0.08, above=6pt] {\edgelabel{$s, b_0, b_2$}} ()
        (s1) edge[bend left=28] node[pos=0.55, above=2pt] {\edgelabel{$b_1$}} (s2)
        (s2) edge[bend left=28] node[pos=0.55, below=2pt] {\edgelabel{$b_0, b_2$}} (s1)
        (s2) edge[bend left=22] node[pos=0.35, left=10pt] {\edgelabel{$s$}} (s3)
        (s3) edge[bend left=22] node[pos=0.65, right=10pt] {\edgelabel{$b_1$}} (s2)
        (s3) edge[bend left=22] node[pos=0.55, below=2pt] {\edgelabel{$b_0, b_2$}} (s1)
        (l) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$b_1$}} ()
        (l) edge node[midway, above=-1pt] {\edgelabel{$s$}} (m2)
        (r) edge[loop right] node[pos=0.12, right=5pt] {\edgelabel{$b_2$}} ()
        (r) edge node[midway, right=-1pt] {\edgelabel{$s$}} (b)
        (m2) edge[bend left=22] node[midway, left=-1pt] {\edgelabel{$b_1$}} (l.south)
        (m2) edge[bend left=22] node[midway, above=3pt, right=1pt] {\edgelabel{$b_0, b_2$}} (r)
        (m2) edge node[midway, left=-1pt] {\edgelabel{$s$}} (b)
        (b) edge[loop right] node[pos=0.12, right=5pt] {\edgelabel{$s$}} ();
      \draw[->] (b.north west) to[out=125, in=225, looseness=1.2]
        node[pos=0.38, left=2pt] {\edgelabel{$b_1$}} (l.south west);
      \draw[->] (b.north east) to[out=55, in=315, looseness=1.2]
        node[pos=0.38, right=2pt] {\edgelabel{$b_0, b_2$}} (r.south east);
    \end{scope}

    \node[font=\scriptsize, align=left, fill=white, inner sep=3pt, rounded corners=1pt]
      at (5.35, 1.05) {%
      $\begin{aligned}
        \{1, 2\} &\mapsto b_1 \\
        \{2\}    &\mapsto b_2 \\
        \varnothing &\mapsto b_0 \\[0.2em]
        \Gamma_1 &= \{b_0, b_1, b_2\}
      \end{aligned}$};
  \end{tikzpicture}
  \end{adjustbox}
  \caption{Пример аннотированного ДКА (слева) для 1-шаблонов. Справа --- упрощённый распознаватель над алфавитом $\{s\} \cup Q_2 / \sim_\alpha$.}
  \label{fig:simplified-pair-level1}
\end{figure}
\begin{figure}[htbp]
  \centering
  \begin{adjustbox}{max width=\textwidth,center}
  \begin{tikzpicture}[
    >=Stealth,
    auto,
    font=\footnotesize,
    initial text={},
    initial by arrow,
    initial where=above,
    initial distance=7mm,
    every state/.style={thick, fill=gray!10, circular drop shadow,
      minimum size=7.5mm, inner sep=0pt, font=\scriptsize},
    no close state/.style={fill=blue!15},
    every loop/.style={min distance=6mm, looseness=5},
  ]
    \begin{scope}[
      every state/.append style={dashed},
    ]
      \def\colsep{2.2cm}
      \def\vdist{1.35cm}

      \node[state, initial] (q0) at (0, 2*\vdist) {};
      \node[state, no close state] (q1) at (0, \vdist) {};
      \node[state] (q2) at (\colsep, \vdist) {};
      \node[state] (q3) at (0, 0) {};
      \node[state, no close state] (q4) at (2*\colsep, \vdist) {};
      \node[state, no close state, accepting] (qA) at (2*\colsep, 0) {};
      \node[state, no close state, accepting] (qB) at (0, -\vdist) {};
      \node[font=\scriptsize, below=3pt of qA] {$\{A\}$};
      \node[font=\scriptsize, below=3pt of qB] {$\{B\}$};

      \path[->]
        (q0) edge node[midway, left=-1pt] {\edgelabel{$\litparen$}} (q1)
        (q1) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litrep;\,\overline{2,3}$}} (q2)
        (q1) edge node[midway, left=-1pt] {\edgelabel{$\litrep;\,2,3$}} (q3)
        (q2) edge node[pos=0.55, above=-1pt] {\edgelabel{$\litparen$}} (q4)
        (q3) edge node[midway, left=-1pt] {\edgelabel{$s$}} (qB)
        (q4) edge node[midway, right=-1pt] {\edgelabel{$\litrep;\,1$}} (qA);
      \draw[->] (q3.east) to[out=0, in=225, looseness=0.85]
        node[pos=0.48, below=1pt] {\edgelabel{$\litparen$}} (q4.south west);
    \end{scope}

    \begin{scope}[xshift=7.2cm]
      \def\colsep{2.2cm}
      \def\vdist{1.35cm}

      \node[state, initial] (r0) at (0.5*\colsep, 2*\vdist) {};
      \node[state] (r1) at (0, \vdist) {};
      \node[state] (r2) at (\colsep, \vdist) {};
      \node[state, accepting] (rA) at (0, 0) {};
      \node[state, accepting] (rB) at (\colsep, 0) {};
      \node[font=\scriptsize, below=3pt of rA] {$\{A\}$};
      \node[font=\scriptsize, below=3pt of rB] {$\{B\}$};

      \path[->]
        (r0) edge node[midway, left=-1pt] {\edgelabel{$b_2, b_0$}} (r1)
        (r0) edge node[pos=0.42, right=-1pt] {\edgelabel{$b_1, b_3$}} (r2)
        (r1) edge node[midway, left=-1pt] {\edgelabel{$b_2$}} (rA)
        (r2) edge[bend left=18] node[pos=0.45, above=1pt] {\edgelabel{$b_2$}} (rA)
        (r2) edge node[midway, right=-1pt] {\edgelabel{$s$}} (rB);
    \end{scope}

    \node[font=\scriptsize, align=left, fill=white, inner sep=3pt, rounded corners=1pt]
      at (5.75, -3.05) {%
      $\begin{aligned}
        \{\mathrm{I},\,\mathrm{II}\} &\mapsto b_1 \\
        \{\mathrm{I}\}               &\mapsto b_2 \\
        \{\mathrm{II}\}              &\mapsto b_3 \\
        \varnothing                   &\mapsto b_0 \\[0.2em]
        \Gamma_1 &= \{b_0, b_1, b_2, b_3\}
      \end{aligned}$};
  \end{tikzpicture}
  \end{adjustbox}
  \caption{Последний элемент каскада уровня~0 (слева). Справа --- упрощённый автомат-распознаватель над алфавитом $\{s\} \cup Q_1 / \sim_\alpha$.}
  \label{fig:simplified-pair-level0}
\end{figure}

Заметим, что по построению общее число аннотаций на каждого уровне относительно числа $k$-шаблонов $n$ есть $O(2^n).$
Именно этим обусловлен отказ от рассмотрения шаблонов, содержащих атомарные символы --- в шаблонах с высоким уровнем вложенности 
различие констант может привести к очень сильному разрастанию алфавита.

\subsection{Сужение шаблона как регулярный язык} % 1 стр.

Используя конструкцию ДКА из раздела выше для построения автоматов по набору шаблонов 
в функции, и используя базовые операций над этими ДКА, можно построить сужения каждого шаблона.

Упрощенная задача экранирования для функции $f$ теперь тривиально решается проверкой $\Fact(f.p_i) = \varnothing$ для всех $p_i \in \pat f$.

Для полного решения задачи экранирования для функции $f$, решим сначала упрощенный вариант задачи, а потом для каждого экранируемого шаблона $p_i$ рассмотрим копию 
функции $f$, из которой поочередно будем пытаться удалять шаблоны $p_{i-1}, p_{i-2}, \dots, p_1$. Если при удалении очередного шаблона $f.p_j$ и последующем решении упрощенной задачи экранирования для оставшейся части функции $f$ результат будет пустым множеством, то шаблон $p_j$ участвует в экранировании шаблона $p_i$ и остается в функции. В противном случае он удаляется.

Теперь же рассмотрим сужения в контексте извлечения документации о шаблоне.

Рассмотрим пример гипотетической функции, приведённой \inlisting{lst:prefix1}.
\begin{listing}[H]
\caption{Простой пример уточняемого шаблона в определении функции (в диалекте \RL)}
\label{lst:prefix1}
\begin{minted}{refal}
AnalyzeData {
  t.node /* empty */ = <AnalyzeData <AppendData t.node>>;
  t.node t.data, t.data : {
    ...
  };
  e.otherwise = <Error>;
}
\end{minted}
\end{listing}

В данном примере очевидно, что шаблон третьего предложения $AnalyzeData.p_3$ можно уточнить:
$$
\texttt{e.otherwise} \xrightarrow{\sim}  \texttt{t.1 t.2 t.3 e.4};
$$
т.е. сужение шаблона $\texttt{e.otherwise}$ совпадает с $\Lang(\texttt{t.1 t.2 t.3})$.

Менее очевидный пример представлен \inlisting{lst:prefix2}.
Для последнего шаблона можно вывести, что все слова, сопоставляющиеся с этим шаблоном, или начинаются 
с пустых скобок $\litparen \litrep$, либо со слов, удовлетворяющих шаблону $\texttt{\litparen \litparen t.1 e.2 \litrep e.3 \litrep}$.

\begin{listing}[H]
\caption{Нетривиальный пример уточняемого шаблона в определении функции}
\label{lst:prefix2}
\begin{minted}{refal}
h {
  () t.1 = ...;
  (e.1 () e.3) e.4 = ...;
  (s.1 e.1) e.2 = ...;
  (e.1) e.2 s.1 e.3 = ...;
  (e.1) e.2 = ...;
}
\end{minted}
\end{listing}

Обходя в ширину ДКА фактического языка $0$-шаблонов можно строить дерево обязательных префиксов.
Помимо этого, можно извлекать обязательные инфиксы. Это простое наблюдение нуждается в аккуратной реализации: в самом наглядном представлении обязательных префиксов --- перечислении --- может оказаться слишком много элементов. Из-за сложности нахождения 
всех обязательных инфиксов для данного ДКА, разумно использовать аппроксимацию.
ДКА можно представить как граф, вершины которого есть состояния ДКА, а ребра --- переходы между состояниями (переходы между одинаковыми состояниями по разным символам схлопываются в одно ребро, которое помечается всеми этими символами). С помощью классического алгоритма Тарьяна в графе можно найти мосты. Дополнительно проверив, 
что ни одно финальное состояние не достигается без посещения \enq{начала} моста, можно 
получить некоторые обязательные инфиксы языка ДКА.

\section{Конструкторская часть}

\subsection{Общая архитектура программного комплекса} % 2 стр.

Было решено работать с диалектом \RL, поскольку этот диалект, во-первых, является точным 
надмножеством Рефала-5, а во-вторых является современной реализацией Рефала, знакомой автору.

Рационально поделить программный комплекс на две части --- библиотеку, содержащую всю 
логику работы с шаблонами, автоматами, алгоритмами и т.п., и приложение с интерфейсом командной строки, использующую 
эту библиотеку.

Устройство самой библиотеки следует классической архитектуре большинства фронтендов компиляторов. 
Таким образом, библиотека делится на три модуля:
\begin{itemize}
\item модуль \texttt{lexer} --- модуль, отвечающий за лексический анализ;
\item модуль \texttt{parser} --- модуль, отвечающий за синтаксический анализ;
\item модуль \texttt{sema} --- модуль, отвечающий за семантический анализ.
\end{itemize}

Модуль \texttt{sema} единственный предоставляет публичные функции, доступные для вызова извне.
Сами модули взаимодействуют друг с другом весьма ограниченно, сохраняя свою внутреннюю логику закрытой.
Схема взаимодействия модулей и пользователя представлена \onfigure{fig:arch-modules}.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    >=Stealth,
    module/.style={
      draw=black!70, thick, rounded corners=4pt, fill=gray!8,
      minimum width=6.5cm, minimum height=1.1cm,
      align=center, inner sep=3pt, font=\normalsize
    },
    arrowlabel/.style={
      font=\small, inner sep=2pt
    },
  ]
    \node[module] (lexer) {лексический анализатор};
    \node[module, below=1.65cm of lexer] (parser) {синтаксический анализатор};
    \node[module, below=1.65cm of parser] (sema) {семантический анализатор};
    \node[module, below=1.65cm of sema] (cli) {приложение с интерфейсом командной строки};
    \node[module, below=1.65cm of cli] (user) {пользователь};

    \draw[->, thick] (lexer.south) -- (parser.north)
      node[midway, arrowlabel, fill=white] {набор токенов и список ошибок};
    \draw[->, thick] (parser.south) -- (sema.north)
      node[midway, arrowlabel, fill=white] {синтаксическое дерево и список ошибок};
    \draw[->, thick] (sema.south) -- (cli.north)
      node[midway, arrowlabel, fill=white] {семантическая информация};
    \draw[->, thick] (cli.south) -- (user.north)
      node[midway, arrowlabel, fill=white] {человекочитаемое представление семантической информации};
  \end{tikzpicture}
  \caption{Схема взаимодействия модулей программного комплекса.}
  \label{fig:arch-modules}
\end{figure}

Лексический и синтаксический анализаторы не представляют особого интереса --- \RL имеет сравнительно 
простую лексическую структуру, а грамматика языка~\cite{refal5lambda_breference} доускает реализацию синтаксического анализа 
рекурсивным спуском. Так как основная цель работы --- написание семантического анализатора, лексический и 
синтаксический анализаторы реализованы довольно просто, и не имеют продвинутых способов восстановления после ошибок.

\subsection{Архитектура подсистемы семантического анализатора}

\subsubsection{Вспомогательные подмодули семантического анализатора} % 2-3 стр.

Семантический анализатор, будучи самым крупным модулем библиотеки, содержит в себе несколько подмодулей, 
выполняющих различные задачи.

Первым таким подмодулем является модуль \texttt{automata}, отвечающий за работу с автоматами и за способ хранения ДКА и НКА в памяти. При разработке прототипа приложения и его тестировании было установлено, что 
в абсолютном большинстве случаев автоматы имеют размер до десяти тысяч состояний, а размер алфавита не превышает одной тысячи.
В связи с этим в итоге было выбрано сжатое разреженное строковое представление для исходящих и входящих ребер. Пример такого представления только для исходящих ребер приведен \onfigure{fig:dfa-csr} (для входящих ребер схема аналогична). Отслеживать входящие ребра легко при построении ДКА, в дальнейшем это дает выигрыш при реализации алгоритмов минимизации и удаления бесполезных состояний.

\begin{figure}[H]
\centering
\small
\begin{adjustbox}{width=\textwidth,center}
\begin{tikzpicture}[
  >=Stealth,
  auto,
  font=\footnotesize,
  initial text={},
  every state/.style={thick, fill=gray!10, circular drop shadow,
    minimum size=10mm, inner sep=0pt, font=\scriptsize},
  every loop/.style={min distance=8mm, looseness=5},
  memcell/.style={draw, minimum width=7mm, minimum height=6mm, inner sep=0pt, font=\scriptsize},
  idxcell/.style={font=\tiny, anchor=center},
  brace/.style={decorate, decoration={brace, amplitude=4pt}, thick},
  lbl/.style={font=\scriptsize},
  arrlbl/.style={anchor=east, font=\footnotesize},
]

% ---------- Левая часть: граф ДКА (сетка 2×2) ----------
\def\stX{2.8}
\def\stY{2.5}

\node[state, fill=blue!15]            (q0) at (0, 0)       {$q_0$};
\node[state, fill=orange!20]          (q1) at (0, \stY)    {$q_1$};
\node[state, fill=green!15]           (q2) at (\stX, \stY) {$q_2$};
\node[state, accepting, fill=red!15]  (q3) at (\stX, 0)    {$q_3$};

\draw[->, thick] (-0.85, -0.55) -- (q0.220);

\path[->, thick]
  (q0) edge[loop left] node[pos=0.12, left=5pt] {\edgelabel{$b$}} ()
  (q0) edge node[left=4pt] {\edgelabel{$a$}} (q1)
  (q0) edge[bend right=22] node[right=4pt] {\edgelabel{$c$}} (q2)
  (q1) edge[loop above] node[pos=0.12, above=5pt] {\edgelabel{$a$}} ()
  (q1) edge[bend left=14] node[left=4pt] {\edgelabel{$b$}} (q3)
  (q2) edge node[above=4pt] {\edgelabel{$a$}} (q1)
  (q2) edge[loop above] node[pos=0.12, above=5pt] {\edgelabel{$c$}} ()
  (q3) edge node[below=4pt] {\edgelabel{$b$}} (q0);

% ---------- Правая часть: CSR в памяти ----------
\begin{scope}[xshift=7.2cm, yshift=0.3cm]

\def\cellStep{0.85}
\def\cellHalf{0.35} % половина ширины memcell (7mm)
\def\symY{2.2}
\def\tgtY{0.9}
\def\idxY{1.55} % середина между символами и целями

\def\arrLblX{-0.35}

% смещения
\node[arrlbl] at (\arrLblX, 4.2) {смещения:};

\foreach \i/\v in {0/0, 1/3, 2/5, 3/7, 4/8} {
  \node[memcell, fill=gray!12] (off\i) at (\i*\cellStep, 4.2) {\v};
  \node[idxcell] at (\i*\cellStep, 4.65) {\i};
}

% Цветные диапазоны над out_symbols / out_targets
\def\braceY{2.58}
\def\braceLblY{2.88}
\foreach \col/\s/\e/\st in {
  blue!60/0/3/0,
  orange!70!black/3/5/1,
  green!60!black/5/7/2,
  red!70!black/7/8/3
} {
  \draw[\col, brace]
    ({\s*\cellStep-\cellHalf}, \braceY) -- ({(\e-1)*\cellStep+\cellHalf}, \braceY);
  \node[lbl] at ({0.5*(\s+\e-1)*\cellStep}, \braceLblY) {$q_{\st}$};
}

% символы
\node[arrlbl] at (\arrLblX, \symY) {символы:};

\foreach \i/\sym/\col in {
  0/$a$/blue!15,
  1/$b$/blue!15,
  2/$c$/blue!15,
  3/$a$/orange!20,
  4/$b$/orange!20,
  5/$a$/green!15,
  6/$c$/green!15,
  7/$b$/red!15
} {
  \node[memcell, fill=\col] (sym\i) at (\i*\cellStep, \symY) {\sym};
}

% цели
\node[arrlbl] at (\arrLblX, \tgtY) {цели:};

\foreach \i/\tgt/\col in {
  0/$1$/blue!15,
  1/$0$/blue!15,
  2/$2$/blue!15,
  3/$1$/orange!20,
  4/$3$/orange!20,
  5/$1$/green!15,
  6/$2$/green!15,
  7/$0$/red!15
} {
  \node[memcell, fill=\col] (tgt\i) at (\i*\cellStep, \tgtY) {\tgt};
}

\foreach \i in {0,...,7} {
  \node[idxcell] at (\i*\cellStep, \idxY) {\i};
}

\node[lbl, anchor=north west] at (\arrLblX, 0.35)
  {диапазон $q_s$: [смещения[$s$], смещения[$s+1$])};
\node[lbl, anchor=north west] at (\arrLblX, -0.05)
  {переход $(s,a)$: бинарный поиск $a$ в символы[\ldots]};

\end{scope}

\end{tikzpicture}
\end{adjustbox}
\caption{Представления ДКА в памяти}
\label{fig:dfa-csr}
\end{figure}
В числе операций на ДКА, реализованных в модуле, присутствуют:
\begin{itemize}
\item объединение;
\item аннотированное объединение;
\item пересечение;
\item конкатенация (через прямое произведение);
\item конкатенация (через построение НКА);
\item дополнение;
\item проверка языков автоматов на включение;
\item минимизация;
\item удаление бесполезных состояний.
\end{itemize} 
Необходимость двух вариантов конкатенации ДКА будет обусловлена в следующем разделе. НКА в модуле нужны лишь для того, 
чтобы стать результатом конкатенации ДКА и сразу же быть детерминизированными.

Второй подмодуль \texttt{abstract\_patterns} позволяет из конкретных узлов синтаксического дерева строить абстрактные шаблоны, 
эквивалетные шаблонам из $\patLang.$ Наличие такого модуля значительно облегчает дальнейшую обработку шаблонов.
Ключевой функцией в модуле является функция получения из узла синтаксического дерева определения функции набора абстрактных шаблонов 
и сопутствующей информации; наличие этой информации обусловлено необходимостью разлчиать шаблоны, изначально удовлетворяющие 
определению шаблонов из $\patLang$, от остальных. Модуль в том числе несет ответственность за преобразование всех шаблонов к виду, 
описанному в исследовательской части ССЫЛКА!. Информация об абстрактном шаблоне включает в себя следующие факты:
\begin{itemize}
\item были ли в искомом шаблоне повторные переменные;
\item были ли в искомом шаблоне атомарные символы;
\item были ли в предложении функции, соответствующем искомому шаблону, условия.
\end{itemize}

\subsubsection{Проектирование алгоритмов} % 3 стр.

Центральным алгоритмом является построение ДКА абстрактного шаблона. Псевдокод этого алгоритма приведен \inlisting{alg:abstract-pattern-dfa}.

\begin{algorithm}[H]
\caption{Построение ДКА абстрактного шаблона}
\label{alg:abstract-pattern-dfa}
\begin{algorithmic}[1]
\Require Шаблоны $p_1, p_2, \ldots, p_n$
\Ensure ДКА $A_1, A_2, \ldots, A_n$ для шаблонов $p_1, p_2, \ldots, p_n$
\State $d_{\max} \gets$ максимальная глубина подшаблонов
\State $\Gamma \gets \{s, b\}$
\For{$d \in \tuple{d_{\max}, d_{\max}-1, \ldots, 1}$}
    \State $S_d \gets \bigcup_{i=1}^{n} \subpat_d(p_i)$ \Comment{Инвариант цикла --- $S_d$ не содержат скобок}
    \Foreach{$p \in S_d$}
        \State Построить ДКА $A_p$ над $\Gamma$ через конкатенацию ДКА-фрагментов
    \EndForeach
    \State $A \gets \textsc{АннотированноеОбъединение}(\{A_p : p \in S_d\})$
    \State $\Gamma \gets \{s\} \cup Q(A) / {\sim_\alpha}$
    \If{в $A$ есть полезное непринимающее состояние}
        \State $\Gamma \gets \Gamma \cup \{\varnothing\}$
    \EndIf
    \Foreach{$\mathit{pat} \in \{p_1, \ldots, p_n\}$}
        \Foreach{$p \in S_d$}
            \State Заменить вхождение $p$ в $\mathit{pat}$ на регулярное выражение $r = (I_1 \mid I_2 \mid \cdots \mid I_m)$, где $I_i \in \Gamma$ входит в альтернативу тогда и только тогда, когда $p \in \alpha(I_i)$
        \EndForeach
    \EndForeach
\EndFor
\Foreach{$p_i \in \{p_1, \ldots, p_n\}$}
    \State Построить ДКА $A_i$ над $\Gamma$ через конкатенацию ДКА-фрагментов
\EndForeach
\State \Return $A_1, A_2, \ldots, A_n$
\end{algorithmic}
\end{algorithm}

В алгоритме построения ДКА под ДКА-фрагментами понимаются небольшие ДКА, 
построение которых занимает $O(|\Gamma|)$ времени. Так, для \texttt{t} автомат будет 
содержать два состояния: непринимающее начальное и принимающее, причем из первого во второго есть переходы по всем символам алфавита. Для \texttt{e} автомат будет содержать одно состояние: принимающее, из которого есть петли по всем символам алфавита. Для \texttt{s} автомат будет содержать два состояния: принимающее и непринимающее начальное, из которого есть переход по символу $s$. Для альернативы 
$(\gamma_1 \mid \gamma_2 \mid \cdots \mid \gamma_m)$ автомат будет содержать 
два состояния (начальное непринимающее и принимающее), между которыми есть переходы по $\gamma_1, \gamma_2, \ldots, \gamma_m$.

Другим важным алгоритмом, эксплуатирующим предыдущий, является построение сужения шаблона \inlisting{alg:pattern-narrowing}.

\begin{algorithm}[H]
\caption{Построение сужений шаблонов функции}
\label{alg:pattern-narrowing}
\begin{algorithmic}[1]
\Require Шаблоны $p_1, p_2, \ldots, p_n$ функции в порядке сверху вниз
\Ensure ДКА $F_1, F_2, \ldots, F_n$ такие, что $\Lang(F_i) = \Fact(p_i)$
\State $(A_1, \ldots, A_n) \gets \textsc{ПостроениеДКА}(p_1, \ldots, p_n)$ \Comment{см. листинг~\ref{alg:abstract-pattern-dfa}}
\State $A_{\text{acc}} \gets$ ДКА с пустым языком над алфавитом $A_1$
\Foreach{$i \in \overline{1, n}$}
    \State $F_i \gets A_i \cap \overline{A_{\text{acc}}}$
    \State $A_{\text{acc}} \gets A_{\text{acc}} \cup A_i$
\EndForeach
\State \Return $F_1, F_2, \ldots, F_n$
\end{algorithmic}
\end{algorithm}

Имея ДКА абстрактного шаблона, можно использовать их для решения задачи распознавания перекрытий и получения документации.

Отметим, что сужать фактический язык шаблона могут лишь те шаблоны, язык которых \enq{не искажен} различным образом. Поэтому в алгоритме участвуют в сопоставлении фактического языка только те шаблоны, которые изначально удовлетворяли определению шаблонов языка $\patLang$ (абстрактизация конкретного шаблона всегда расширеят его язык). Еше одно требование --- в предложениях шаблонов, участвующих в сужении, не должно быть условий (условия обычно искажают язык шаблона $p$ в том смысле, что множество распознаваемых им слов из-за условия является лишь подмножеством $\Lang(p)$). Именно по этой причине необходимо сохранять информацию об условиях в абстрактных шаблонах.

Алгоритм решения упрощенной задачи распознавания перекрытий представлен \inlisting{alg:simple-overlap}.

\begin{algorithm}[H]
\caption{Упрощённая задача распознавания перекрытий}
\label{alg:simple-overlap}
\begin{algorithmic}[1]
\Require Шаблоны $p_1, p_2, \ldots, p_n$ функции в порядке сверху вниз
\Ensure $I \subseteq \overline{1, n}$ --- множество индексов перекрываемых предложений
\State $(F_1, \ldots, F_n) \gets \textsc{ПостроениеСужений}(p_1, \ldots, p_n)$ \Comment{листинг~\ref{alg:pattern-narrowing}}
\State $I \gets \varnothing$
\Foreach{$i \in \overline{1, n}$}
    \If{$\Lang(F_i) = \varnothing$}
        \State $I \gets I \cup \{i\}$
    \EndIf
\EndForeach
\State \Return $I$
\end{algorithmic}
\end{algorithm}

Алгоритм решения полной задачи распознавания перекрытий представлен \inlisting{alg:full-overlap}.

\begin{algorithm}[H]
\caption{Полная задача распознавания перекрытий}
\label{alg:full-overlap}
\begin{algorithmic}[1]
\Require Функция $f$ с шаблонами $p_1, p_2, \ldots, p_n$
\Ensure Для каждого $i \in I$ --- минимальное по включению множество $I_i \subseteq \overline{1, i-1}$
\State $I \gets \textsc{РаспознаваниеПерекрытий}(p_1, \ldots, p_n)$ \Comment{листинг~\ref{alg:simple-overlap}}
\State $\mathcal{I} \gets \varnothing$
\Foreach{$i \in I$}
  \State $S \gets \tuple{f.p_1, f.p_2, \ldots, f.p_{i-1}}$
  \For{$j \in \tuple{i-1, i-2, \ldots, 1}$}
    \State $S' \gets S$ без паттерна $f.p_j$
    \State $(F_1, F_2,\ldots, F_{n'}) \gets \textsc{ПостроениеСужений}(S')$
    \Comment{листинг~\ref{alg:pattern-narrowing}}
    \If{$\Lang(F_n') = \varnothing$}
      \State $S \gets S'$
    \EndIf
  \EndFor
  \State $I_i \gets \{ j \in \overline{1, i-1} \mid p_j \in S \}$
\EndForeach
\end{algorithmic}
\end{algorithm}

Обратим внимание, что при работе с реальными программами целесообразно сначала находить решение 
упрощенной задачи, так как экранирование не является часто встречающимся явлением, а потом уже для 
функций, в которых встречается экранирование, находить решение полной задачи.
Обусловлено это сложностью построения ДКА (в том числе промежуточных) с большим алфавитом (размер которого может в худшем случае быть экспоненциальным от количества различных шаблонов данного уровня вложенности). Операции над ДКА, необходимые для построения сужений, выполняются гораздо быстрее, но, тем не менее, если для решения классической задачи требуется порядка $O(N)$ операций для некоторого $N$, то для решения полной задачи сужения требуется уже порядка $O(N^2)$ операций.

Если сужение шаблона непусто, можно извлекать некоторые факты о нем. 
Одним из вариантов является анализ множества обязательных префиксов и обязательных инфиксов.

Дерево обязательных префиксов, построенное для ДКА шаблона 0-го уровня вложенности, может содержать символы $b_1, b_2, \dots, b_n$.
Для человека такие символы никакой смысловой нагрузки почти не несут, поэтому для каждого символа строится свое дерево обязательных префиксов. Это продолжается до тех пор, пока не будет получено дерево над алфавитом $\{s, b\}$. Однако в результате склейки таких деревьев результирующее дерево может оказаться слишком большим. Для баланса между полнотой документации и ее размера используется эвристика, ограничивающая 
деревья на длину веток и число веток в целом. Причем чем выше уровень вложенности символа $b_i$, тем сильнее 
ограничивается число веток, а на длину ограничение небольшое. Чем ниже уровень вложенности символа $b_i$, тем сильнее 
ограничивается длина веток, на число веток накладывается все меньшее ограничение.

Алгоритм извлечения обязательных инфиксов работает с ДКА как с графом, при этом кратные ребра схлопываются в одно.

Два алгоритма извлечения документации к шаблонам функции представлены в листингах ЛИСТИНГ! и ЛИСТИНГ!.

\subsection{Пользовательский интерфейс} % 2 стр.
Пользовательский интерфейс обязан предоставлять пользователю возможность воспользоваться основными функциями 
модуля семантического анализатора, а именно решениями задачи распознавания перекрытий и извлечения документации к шаблонам функции.

Как уже было сказано, библиотеке сопутствует приложение с интерфейсом командной строки, использующее эту библиотеку.
Это приложение позволяет пользователю указать файл с исходным кодом и выполнить одно из трех основных действий:
\begin{itemize}
\item решение упрощенной задачи распознавания перекрытий;
\item решение полной задачи распознавания перекрытий;
\item извлечение документации к шаблонам функции;
\end{itemize}

Для простоты взаимодействия с приложением весь вывод направляется в стандартный поток вывода в удобном для чтения формате.
Таким образом, пользовательский интерфейс представляет собой интерфейс командной строки, позволяющий пользователю взаимодействовать с библиотекой с помощью указания флагов при запуске программы.

\section{Технологическая часть}

\subsection{Обзор используемых технологий} % 2 стр.

\subsection{Реализация модуля для работы с абстрактными шаблонами} % 2 стр.

\subsection{Реализация модуля для работы с автоматами} % 2 стр.

\subsection{Реализация алгоритма обнаружения перекрытий} % 2 стр.

\subsection{Реализация алгоритма извлечения документации} % 3 стр.

\subsection{Руководство пользователя} % 2 стр.
% что и как установить
% git
% что как запускат

\section{Аналитическая часть}
% WiP

\anonsection{ЗАКЛЮЧЕНИЕ} % 1 стр.

\anonsection{СПИСОК ЛИТЕРАТУРЫ} % 1 стр.

\printbibliography[title={Список литературы},heading=bibintoc]

\end{document}
